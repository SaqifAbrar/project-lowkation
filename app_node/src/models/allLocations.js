import {getAllLocations, categoryBasedSearch, searchTags, searchBasedOffName, cityBasedSearch, topLocations, getAllLocations} from './locationDBQueries.js'


getAllLocations();// maybe make it so that when user makes account or levels up a query is ran and we collect all locations with the rank they leveled up to